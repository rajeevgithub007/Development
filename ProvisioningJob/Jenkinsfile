pipeline {
    agent any

    stages {
        stage('Provisioning') {
            steps {
                script {
                    nodejs("Node-18.14.1") {
                     sh 'yarn install'
                    }
                    // dir('../') {
                    //     // Run the 'make' command
                    //     sh 'make'
                    // }
                }
                echo 'Provisioning Jenkinsfile - Provisioning job is working'
            }
        }
    }
}
